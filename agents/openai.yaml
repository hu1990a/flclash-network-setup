interface:
  display_name: "FlClash 小白网络向导"
  short_description: "一次性配置网络、批测节点并按需复检"
  default_prompt: "Use $flclash-network-setup to test and recommend a stable low-risk node, complete the one-time network setup, and run redacted verification only when I explicitly request it."

policy:
  allow_implicit_invocation: true
